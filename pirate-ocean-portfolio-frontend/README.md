# Pirate Ocean Portfolio Frontend

This repo owns the UI only:

- Next.js app routes and layouts
- styling and design system
- animations and 3D presentation
- client-side state and data fetching

## Local development

```bash
pnpm install
pnpm dev
```

Create `.env.local` from `.env.example` and point `NEXT_PUBLIC_API_URL` at the backend.
